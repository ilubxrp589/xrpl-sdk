[package]
name = "xrpl-worker"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
worker = "0.3"
xrpl-core = { path = "../../crates/xrpl-core", features = ["wasm"] }
hex = "0.4"
serde_json = "1"
serde = { version = "1", features = ["derive"] }
